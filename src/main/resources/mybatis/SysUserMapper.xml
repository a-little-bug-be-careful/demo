<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo2.mapper.SysUserMapper">

    <resultMap type="SysUser" id="SysUserResult">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="passWord" column="password"/>
        <result property="status" column="status"/>
        <result property="loginIp" column="login_ip"/>
        <result property="updateBy" column="update_by"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="getSysUserList" parameterType="SysUser" resultMap="SysUserResult">
        select *
        from sys_user
        <where>
            <if test="id != null and id != ''">AND ID = #{id}</if>
            <if test="userName != null and userName != ''">AND user_name = #{userName}</if>
            <if test="passWord != null and passWord != ''">AND password = #{passWord}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
            <if test="updateBy != null and updateBy != ''">AND update_by = #{updateBy}</if>
            <if test="createBy != null and createBy != ''">AND create_by = #{createBy}</if>
            <if test="createTime != null">AND create_time = #{createTime}</if>
            <if test="updateTime != null">AND update_time = #{updateTime}</if>
        </where>
    </select>

    <select id="getSysUserById" parameterType="String" resultMap="SysUserResult">
        select *
        from sys_user
        where id = #{id}
    </select>

    <select id="getSysUserByName" parameterType="String" resultMap="SysUserResult">
        select *
        from sys_user
        where user_name = #{userName}
    </select>

    <select id="checkExistSysUser" parameterType="SysUser" resultType="java.lang.Integer">
        select count(*)
        from sys_user
        <where>
            <if test="userName != null and passWord != ''">
                and user_name = #{userName}
            </if>
            <if test="passWord != null and passWord != ''">
                and password = #{passWord}
            </if>
        </where>
    </select>

    <select id="checkExistSysUserByUserName" parameterType="String" resultType="java.lang.Integer">
        select count(*) from sys_user where user_name = #{userName}
    </select>

    <!-- 不插入id字段，默认使用数据库自增主键 -->
    <insert id="insertSysUser" parameterType="SysUser" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user(user_name, password, status, login_ip, update_by, create_by, create_time, update_time)
        values (#{userName}, #{passWord}, #{status}, #{loginIp}, #{updateBy}, #{createBy}, sysdate(), sysdate())
    </insert>

    <delete id="deleteSysUserById" parameterType="String">
        delete
        from sys_user
        where id = #{id}
    </delete>

    <delete id="deleteSysUsers" parameterType="java.util.List">
        delete from sys_user where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="updateSysUser" parameterType="SysUser">
        update sys_user
        <set>
            <if test="userName != null and userName != ''">user_name = #{userName},</if>
            <if test="passWord != null and passWord != ''">password = #{passWord},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="loginIp != null and loginIp != ''">login_ip = #{loginIp},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="createBy != null and createBy != ''">create_by = #{updateBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <update id="updateSysUserByUserName" parameterType="SysUser">
        update sys_user
        set password = #{passWord}
        where user_name = #{userName}
    </update>
</mapper>